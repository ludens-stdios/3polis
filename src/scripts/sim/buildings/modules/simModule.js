import { City } from "../../city.js";

export class SimModule {
  simulate(city) {
    // Override in subclasses
  }

  dispose() {
    // Override in subclasses
  }

  toHTML() {
    // Override in subclasses
  }
}
